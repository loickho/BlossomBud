import './MenuBar.css';

export default function MenuBar () {
  return (
    <div className="menu-bar">
      <button>Account</button>
      <button>Home</button>
      <button>Add</button>
    </div>
  )
}